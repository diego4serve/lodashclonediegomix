# Lodash Clone Diego Mix

Install

```shell
npm install lodashclonediegomix
```

Using the library

```javascript
const _ = require('lodashclonediegomix')

const array = _.map([0,1,2], (n) => n * n)
```